look into renaming cachefile to .cachefile
	will need to patch something that isn't a shell script
rename Logs to .logs

versioning
	MinUI-0.105-20210306.zip

rescue things from old repo
	art
	todos

pull in (need to find comparable source)
	fceux
	snes9x4d
	picodrive
	oswan
	pcsx4all
	temper